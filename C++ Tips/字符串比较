一、直接使用运算符
逐字符比较ascii值，ascii对应十进制数大的其在比较中也更大。
只要有一个下标的字符可以完成比较，即根据该字符的ascii大小关系进行返回。
如果两个字符串不一样长，在公共部分如果能比较出大小即返回，如果公共部分完全一样则较长的字符串要“大于”另一个。

二、compare函数
string s1,s2;
int res = s1.compare(s2);
比较的标准和直接使用运算符一样
res>0表示s1大于s2，res==0表示二者相等，res<0表示s1小于s2

三、strcpm函数
strcmp(s1.c_str(),s2.c_str());
比较的标准和直接使用运算符一样
res>0表示s1大于s2，res==0表示二者相等，res<0表示s1小于s2
